apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-config
  namespace: kagent
data:
  alertmanager.yml: |-
    global:
      resolve_timeout: 5m
    route:
      receiver: blackhole
    receivers:
      - name: blackhole
