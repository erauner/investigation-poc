apiVersion: v1
kind: Service
metadata:
  name: investigation-mcp-server
  namespace: kagent
  labels:
    app.kubernetes.io/name: investigation-mcp-server
spec:
  selector:
    app.kubernetes.io/name: investigation-mcp-server
  ports:
    - name: mcp
      port: 8001
      targetPort: mcp
