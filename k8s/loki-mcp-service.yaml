apiVersion: v1
kind: Service
metadata:
  name: loki-mcp-server
  namespace: kagent
  labels:
    app.kubernetes.io/name: loki-mcp-server
spec:
  selector:
    app.kubernetes.io/name: loki-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: http
