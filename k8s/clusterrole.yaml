apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: investigation-service-readonly
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log", "services", "events", "nodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["homelab.erauner.dev"]
    resources: ["backends", "frontends", "clusters"]
    verbs: ["get", "list", "watch"]
