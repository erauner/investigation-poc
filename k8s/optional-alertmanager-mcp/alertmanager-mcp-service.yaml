apiVersion: v1
kind: Service
metadata:
  name: alertmanager-mcp-server
  namespace: kagent
  labels:
    app.kubernetes.io/name: alertmanager-mcp-server
spec:
  selector:
    app.kubernetes.io/name: alertmanager-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: http
